# Paper-Agent 评估与节点门禁

本文档与代码一致：**第一层**为各主节点产出的**确定性门禁**（`src/core/node_gates.py`），写入 `PaperAgentState.boundary_checks`；**第二层**为工作流结束后的**少量 LLM-as-Judge**（`src/evaluation/evaluators.py`）。LangSmith **Tracing 保留**，评估输出结构收敛为面试可讲版本。

## 1. 节点边界（契约）

职责与「不负责」见 `src/core/node_contracts.py` 中 `NODE_CONTRACTS`（search / reading / analyse / writing / report）。面试可答：

| 节点 | 负责 | 不负责 |
|------|------|--------|
| search | 意图→arXiv 可执行条件、拉候选论文、HITL | 全文抽取、跨论文结论、终稿 |
| reading | 单篇结构化抽取、Pydantic 校验、临时库入库 | 聚类/全局结论、写作、终稿 |
| analyse | 聚类、深度分析、全局整合 | 章节长文、Markdown 流式终稿 |
| writing | 大纲、拆 section、并行写作+RAG+审查 | 终稿拼装与流式交付 |
| report | 章节→整篇 Markdown、过渡与润色 | 发明新研究结论 |

## 2. 节点门禁（如何判定「通过」）

门禁在**节点产出之后**立即执行，结果为 `passed` / `score` / `reasons` / `metrics` / `next_action`，写入 `state.boundary_checks[node_name]`。阈值来自 `system_params.yaml` 的 `node_gates.*`。

- **search**：`querys` 非空、无 CJK 子式、日期格式合法、结果数>0、去重后不超过上限；失败写 `search_node_error` 并阻断。
- **reading**：解析率、关键字段完整率、入库数；失败写 `reading_node_error`。
- **analyse**：`cluster_summaries` 非空且含 theme/keywords、`global_analyse` 长度与模块关键词命中；失败写 `analyse_node_error`。
- **writing**：`sections` 非空、章节长度与匹配率、`review_verdict` 统计（见下）；失败写 `writing_node_error`。
- **report**：Markdown 长度、`#` 标题、与源章节覆盖启发式；失败写 `report_node_error`。

**审查结构化输出**：`review_agent` 使用 `ReviewDecision`（`verdict`: pass/revise/fail），并由 `parallel_writing_node` 回写到 `SectionState.review_verdict`，供 writing 门禁计算 `review_pass_rate`。`verdict=pass` 时 JSON 中含 `termination_phrase: APPROVE` 以满足 `TextMentionTermination("APPROVE")`。

## 3. LangSmith：保留什么

- **Tracing**：`@traceable` 工作流、`chat_completion_text_routed` 各调用仍进入 trace tree（依赖 `LANGCHAIN_TRACING_V2` 等，见 `config` / `.env.example`）。
- **评估 span**：`Run Paper Agent Evaluation`、`Workflow Dual-Head Quality Judge`。
- **Feedback**（若配置了 API Key）：`paper_agent_overall_score` 与 `paper_agent_analysis_quality` 等键。

## 4. 收敛后的 `run_evaluation` 输出

`orchestrator.run` 结束时调用 `run_evaluation`，结果写入 `state.config["evaluation"]`，并通过 SSE `evaluation_complete` 推送。结构为：

```json
{
  "node_gates": { "search": {...}, "reading": {...}, ... },
  "quality_eval": {
    "analysis_quality": 0.0,
    "writing_quality": 0.0,
    "report_completeness": 0.0,
    "rag_quality": 0.0,
    "notes": { "analysis": "", "writing": "", "report_metrics": {}, "rag_metrics": {} }
  },
  "overall_score": 0.0,
  "summary": "..."
}
```

- **node_gates**：来自 `boundary_checks`，与运行时门禁一致，**可复现、可审计**。
- **quality_eval**：一次双头 Judge（`analysis_quality` + `writing_quality`）+ **确定性** `report_completeness` + **启发式** `rag_quality`（基于 `rag_retrieval_logs` 条数），避免多层、难解释的重复指标。
- **overall_score**：`0.5 * gate_mean + 0.5 * quality_mean`（四项质量均值），口径固定、便于面试解释。

## 5. 为什么这套指标「够讲」

1. **边界清晰**：契约 + 门禁回答「负责什么、怎么算过」。
2. **失败可定位**：`reasons` 与 `*_node_error` 非笼统失败文案。
3. **成本可控**：主链路不额外加 Judge；全图结束仅一次双头调用 + 轻量启发式。
4. **与模型策略一致**：轻/中/重任务分流见 `models.yaml` 的 `llm-routing.client_policies` 与 `.env.example`。

## 6. 相关代码路径

| 模块 | 路径 |
|------|------|
| 契约 | `src/core/node_contracts.py` |
| 门禁 | `src/core/node_gates.py` |
| 阈值 | `src/core/system_params.yaml` → `node_gates` |
| 评估 | `src/evaluation/evaluators.py` |
| 编排写回 | `src/agents/orchestrator.py` |
| 审查模型与结构化 | `src/agents/sub_writing_agent/review_agent.py`、`writing_state_models.py` |
| 审查回写 | `src/agents/sub_writing_agent/parallel_writing_node.py` |
| 路由 | `src/core/models.yaml`、`src/core/model_client.py` |
