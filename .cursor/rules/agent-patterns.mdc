---
description: Agent 开发模式与多智能体协作规范，涵盖 AutoGen + LangGraph 的使用约定
globs: "src/agents/**/*.py"
alwaysApply: false
---

# Agent 开发模式规范

## LangGraph 工作流
- 工作流在 `orchestrator.py` 中通过 `StateGraph` 定义
- 每个节点函数签名：`async def xxx_node(state: State) -> State`
- 节点间通过 `State` TypedDict 传递数据，禁止使用全局变量
- 条件边通过 `condition_handler` 根据 `current_step` 和 `NodeError` 路由
- 新增节点时需同步更新 `ExecutionState` 枚举和 `condition_handler` 路由逻辑

## 状态管理
- 全局状态使用 `State` TypedDict（包含 `value: PaperAgentState`）
- 前端推送使用 `BackToFrontData(step, state, data)` 通过 `state_queue` 发送
- 子工作流（如 WritingWorkflow）有独立的 `WritingState`
- 状态更新必须返回新的状态字典，不要直接修改入参

```python
# ✅ 正确 - 更新状态并返回
current_state = state["value"]
current_state.search_results = results
return {"value": current_state}
```

## Agent 创建
- 使用 `ModelClient.create_client()` 或 `create_model_client()` 创建 LLM 客户端
- 不同模块可在 `models.yaml` 中配置独立的模型
- Agent 的系统提示词统一定义在 `src/core/prompts.py`
- 创建 AutoGen Agent 时指定 `name` 和 `system_message`

## 多智能体协作
- 写作模块使用 `SelectorGroupChat` 实现多 Agent 协作
- 终止条件使用 `TextMentionTermination("APPROVE")`
- 协作组包含：writing_agent（写作）、retrieval_agent（检索）、review_agent（审核）
- 新增协作组时参照 `writing_chatGroup.py` 的模式

## 人机交互
- 通过 `WebUserProxyAgent` 实现 Human-in-the-loop
- 使用 `asyncio.Future` 机制等待前端输入
- 前端通过 `/send_input` 接口提交审核结果

## SSE 实时推送
- 节点执行过程中通过 `state_queue.put()` 推送进度
- 推送数据格式：`BackToFrontData(step=步骤名, state=状态, data=数据)`
- 状态类型：`initializing`（初始化）、`thinking`（思考中）、`completed`（完成）等
- 流式 LLM 输出使用 `run_stream()` 逐 chunk 推送

## 错误处理
- 节点内异常捕获后设置 `NodeError`，由 `condition_handler` 路由到 `handle_error_node`
- 错误信息需推送到前端以便用户感知
- 使用 `tenacity` 对 LLM 调用做重试

## 新增 Agent 检查清单
1. 在 `src/agents/` 下创建 Agent 文件
2. 在 `prompts.py` 中添加系统提示词
3. 在 `models.yaml` 中配置模型（可选）
4. 在 `model_client.py` 中添加工厂函数（可选）
5. 在工作流中注册节点和边
6. 更新 `ExecutionState` 和 `condition_handler`
7. 在 `test/` 下添加对应测试
